name: Build Veyon on Windows

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: windows-latest

    steps:
      # -----------------------------------------------------------
      # 1. CHECKOUT REPO
      # -----------------------------------------------------------
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      # -----------------------------------------------------------
      # 2. SETUP VCPKG (Clean Clone + Dynamic Baseline)
      # -----------------------------------------------------------
      - name: Setup VCPKG
        shell: pwsh
        run: |
          # 1. Fix Git Line Endings (CRLF) to prevent corruption
          git config --global core.autocrlf false
          
          # 2. Clone fresh vcpkg to a clean path
          $workDir = "C:\vcpkg_clean"
          New-Item -ItemType Directory -Force -Path $workDir | Out-Null
          cd $workDir
          
          Write-Host "Cloning vcpkg..."
          git clone https://github.com/microsoft/vcpkg.git .
          
          # 3. Checkout a known stable release (April 2024)
          # We know libvncserver works well in this version.
          git checkout 2024.04.26
          
          # 4. CAPTURE THE COMMIT HASH (The "Dynamic Baseline" Fix)
          $vcpkgCommit = git rev-parse HEAD
          Write-Host "Detected VCPKG Baseline Hash: $vcpkgCommit"
          
          # 5. Bootstrap
          .\bootstrap-vcpkg.bat
          
          # 6. Save Environment Variables for later steps
          "VCPKG_ROOT=$workDir" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "VCPKG_BASELINE=$vcpkgCommit" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      # -----------------------------------------------------------
      # 3. CREATE DYNAMIC MANIFEST
      # -----------------------------------------------------------
      - name: Create Dynamic Manifest
        shell: pwsh
        run: |
          # We create the vcpkg.json using the EXACT hash of the tool we just installed.
          # This prevents the "not our ref" error.
          $manifest = @{
            "name" = "veyon-deps";
            "version-string" = "1.0.0";
            "builtin-baseline" = "$env:VCPKG_BASELINE";
            "dependencies" = @(
              "libvncserver",
              "lzo",
              "openssl"
            )
          }
          $manifest | ConvertTo-Json | Set-Content "vcpkg.json"
          
          Write-Host "Generated vcpkg.json content:"
          Get-Content "vcpkg.json"

      # -----------------------------------------------------------
      # 4. INSTALL QT 5.15.2
      # -----------------------------------------------------------
      - name: Install Qt 5.15.2
        id: install-qt
        uses: jurplel/install-qt-action@v4
        with:
          version: 5.15.2
          host: windows
          target: desktop
          arch: win64_msvc2019_64

      # -----------------------------------------------------------
      # 5. SETUP MSVC + NINJA
      # -----------------------------------------------------------
      - name: Setup MSVC
        uses: ilammy/msvc-dev-cmd@v1

      - name: Install Ninja
        uses: seanmiddleditch/gha-setup-ninja@v4

      # -----------------------------------------------------------
      # 6. BUILD QCA
      # -----------------------------------------------------------
      - name: Clone QCA Source
        run: git clone --depth 1 --branch master https://github.com/KDE/qca.git qca_src

      - name: Configure QCA
        shell: pwsh
        run: |
          mkdir qca_build
          cd qca_build
          cmake -G "Ninja" `
            -DCMAKE_BUILD_TYPE=Release `
            -DCMAKE_INSTALL_PREFIX="${{ runner.temp }}/qca-install" `
            -DCMAKE_PREFIX_PATH="${{ steps.install-qt.outputs.qt_dir }}" `
            -DBUILD_PLUGINS=OFF `
            -DQCA_FEATURE_BOTAN=OFF `
            -DQCA_FEATURE_OPENSSL=OFF `
            -DQCA_FEATURE_PKCS11=OFF `
            -DQCA_FEATURE_GNUPG=OFF `
            -DQCA_FEATURE_NSS=OFF `
            -DQCA_FEATURE_GCRYPT=OFF `
            -DBUILD_TESTS=OFF `
            ../qca_src

      - name: Build and Install QCA
        run: |
          cd qca_build
          ninja
          ninja install

      # -----------------------------------------------------------
      # 7. PATCH VEYON
      # -----------------------------------------------------------
      - name: Patch Veyon CMakeLists for Qt5
        shell: pwsh
        run: |
          $file = "CMakeLists.txt"
          (Get-Content $file) -replace 'find_package\(Qt6', 'find_package(Qt5' | Set-Content $file
          Write-Host "Patched Veyon to use Qt5."

      # -----------------------------------------------------------
      # 8. CONFIGURE VEYON
      # -----------------------------------------------------------
      - name: Configure Veyon Build
        shell: pwsh
        run: |
          mkdir build
          cd build
          
          # Use our custom vcpkg root
          cmake -G "Ninja" `
            -DCMAKE_BUILD_TYPE=Release `
            -DCMAKE_TOOLCHAIN_FILE="$env:VCPKG_ROOT/scripts/buildsystems/vcpkg.cmake" `
            -DVCPKG_TARGET_TRIPLET=x64-windows `
            -DCMAKE_PREFIX_PATH="${{ steps.install-qt.outputs.qt_dir }};${{ runner.temp }}/qca-install" `
            ..

      # -----------------------------------------------------------
      # 9. BUILD VEYON
      # -----------------------------------------------------------
      - name: Build Veyon
        run: |
          cd build
          ninja

      # -----------------------------------------------------------
      # 10. ARTIFACTS
      # -----------------------------------------------------------
      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: veyon-windows-build
          path: build/**
